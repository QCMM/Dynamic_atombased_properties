package:
  name: dynamic_qm_properties
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - dynamic_qm_properties

about:
  home: add_url_here
  license: MIT License
